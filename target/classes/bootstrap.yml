spring:
  application: 
    name: calpullix-service-product-classification
  config:
    name: '${spring.application.name}'
  cloud:
    config:
      url: http://localhost:8888
  profiles:
    active: composite
